{
  "name": "kureselhaberler-feed",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "RSS -> Groq (yeniden yazma) -> Unsplash (görsel) -> haberler.json üreten otomasyon hattı.",
  "scripts": {
    "generate": "node scripts/generate-feed.mjs",
    "markets": "node scripts/fetch-markets.mjs"
  },
  "dependencies": {
    "fast-xml-parser": "^4.5.0"
  },
  "engines": {
    "node": ">=20"
  }
}
